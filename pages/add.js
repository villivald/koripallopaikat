import Header from "../components/Header";

export default function Add({ weather }) {
  return (
    <div>
      <Header />
      <h1 style={{ textAlign: "center", marginTop: "20px" }}>
        Add new basketball court
      </h1>
    </div>
  );
}
